(ns com.github.rkaippully.clj-meta.core-test
  (:require [clojure.test :refer :all]
            [com.github.rkaippully.clj-meta.core :refer :all]))


(deftest a-test
  (testing "FIXME, I don't fail."
    (is (true? (= 4 (count (str (= 1 1))))))))
